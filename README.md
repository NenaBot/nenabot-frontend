
  # React UI with Material Design (Copy)

  This is a code bundle for React UI with Material Design (Copy). The original project is available at https://www.figma.com/design/gEpT8Cm0PNdwlDTvDhnVfp/React-UI-with-Material-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  