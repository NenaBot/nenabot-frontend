/**
 * Type definitions for application tab navigation.
 *
 * Defines types for tab identifiers and navigation state within the workflow UI.
 */
export type TabId = 'setup' | 'camera' | 'route' | 'progress' | 'results';
